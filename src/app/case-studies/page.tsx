import Link from "next/link";
import { caseStudies } from "@/lib/case-studies";
export default function CaseStudiesPage(){return <main className="mx-auto max-w-6xl px-4 py-24 md:px-8"><p className="font-mono text-xs uppercase tracking-widest text-orange-200">Case studies</p><h1 className="mt-4 text-5xl font-semibold tracking-[-.06em]">Selected architecture work.</h1><div className="mt-10 grid gap-5 md:grid-cols-2">{caseStudies.map(s=><article className="surface-panel rounded-3xl p-6" key={s.slug}><p className="font-mono text-xs uppercase text-orange-200">{s.category}</p><h2 className="mt-3 text-3xl font-semibold">{s.title}</h2><p className="mt-3 leading-7 text-[#d5dbe3]">{s.summary}</p><div className="mt-5 flex flex-wrap gap-2">{s.tags.map(t=><span className="tag" key={t}>{t}</span>)}</div><Link href={`/case-studies/${s.slug}`} className="cta-button-secondary mt-6">Deep dive</Link></article>)}</div></main>}
